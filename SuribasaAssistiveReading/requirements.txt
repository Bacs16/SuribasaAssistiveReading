Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
ReportLab==4.2.0
Werkzeug==3.0.3
python-dotenv==1.0.1

# Use Vosk as the main fallback; skip Whisper for now to avoid heavy deps
# faster-whisper==1.0.3   <-- comment this out for now

pydub==0.25.1
numpy==1.24.4            # <-- Py 3.8-compatible
# tokenizers==0.14.1     # not needed if we skip faster-whisper
